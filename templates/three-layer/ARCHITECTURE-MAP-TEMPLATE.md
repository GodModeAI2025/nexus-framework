<!--
Nexus Framework - Architecture Map Template (Wayfinder Layer)

This file lives at src/ARCHITECTURE.map in the target project.
It is the canonical answer to "where does X live?"

One row per concept. Updated by every code edit that creates or renames
an entry-point. The agent that edits code also updates this map in the
same edit pass.
-->

# Architecture Map

> Concept-to-file lookup. Grep-friendly. One row per concept.
> Updated on every code edit that creates or renames an entry-point.

| Concept | Entry-point | ADR | How to extend |
|---------|-------------|-----|---------------|
| {concept-name} | `src/{module}/{file}.ts:export class {Name}` | ADR-{nn} | {brief instruction} |
